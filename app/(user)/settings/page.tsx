import RouteScreen from "../../../components/routes/RouteScreen";

export default function settingsPage() {
  return <RouteScreen title="Settings" subtitle="User route is active and wallet-auth protected." />;
}
